{
  "version": "0.3.1",
  "description": "A local-first development command center for Windows: run your whole stack, watch every service, and jump into any terminal.",
  "homepage": "https://github.com/d3velopm3nt/devdeck",
  "license": "MIT",
  "architecture": {
    "64bit": {
      "url": "https://github.com/d3velopm3nt/devdeck/releases/download/v0.3.1/DevDeck_0.3.1_x64-setup.exe",
      "hash": "74b3bb0be682e7b56a9a7ae4c9dd0affe5a45198900996efe184b171339b1f4e"
    }
  },
  "installer": {
    "script": [
      "$setup = (Get-ChildItem $dir -Filter '*-setup.exe' | Select-Object -First 1).FullName",
      "Write-Host 'Running the DevDeck installer (per-user, silent, no admin)...'",
      "Start-Process -FilePath $setup -ArgumentList '/S' -Wait"
    ]
  },
  "uninstaller": {
    "script": [
      "$cands = @(\"$env:LOCALAPPDATA\\DevDeck\\uninstall.exe\", \"$env:LOCALAPPDATA\\Programs\\DevDeck\\uninstall.exe\")",
      "$un = $cands | Where-Object { Test-Path $_ } | Select-Object -First 1",
      "if ($un) { Start-Process -FilePath $un -ArgumentList '/S' -Wait } else { Write-Host 'DevDeck uninstaller not found - remove via Settings > Apps if needed.' }"
    ]
  },
  "checkver": {
    "github": "https://github.com/d3velopm3nt/devdeck"
  },
  "autoupdate": {
    "architecture": {
      "64bit": {
        "url": "https://github.com/d3velopm3nt/devdeck/releases/download/v$version/DevDeck_$version_x64-setup.exe"
      }
    }
  },
  "notes": "DevDeck is installed per-user by its official installer (no admin required). Update with 'scoop update devdeck'."
}
